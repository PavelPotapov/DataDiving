import { TooltipPos } from '../types/Tooltip.types'

export const topTooltipPos: TooltipPos = {
  top: '-1rem',
  transform: 'translateY(-100%)'
}

export const leftTooltipPos: TooltipPos = {
  left: '-1rem',
  transform: 'translateX(-100%)'
}

export const bottomTooltipPos: TooltipPos = {
  bottom: '-1rem',
  transform: 'translateY(100%)'
}

export const rightTooltipPos: TooltipPos = {
  right: '-1rem',
  transform: 'translateX(100%)'
}
